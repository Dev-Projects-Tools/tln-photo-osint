[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/dist"
  functions = "netlify/functions"

[build.environment]
  VITE_API_URL = "https://tln-photo-osint.railway.internal"
  NODE_VERSION = "18.17.0"
